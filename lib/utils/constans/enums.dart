//List of Enums
//They cannot be created inside a class

enum ProductType { single, variable }

enum TextSizes { small, medium, large, smaller }

enum OrderStatus { processing, shipped, delivered }

enum PaymentMethods {
  paypal,
  googlePay,
  applePay,
  visa,
  masterCard,
  creditCard,
  paystack,
  razorPay,
  paytm
}
